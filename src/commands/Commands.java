package commands;

/**
 * A list of all the commands used in the program. Used by the controller to
 * assign the correct operations.
 */
public enum Commands {
  EXECUTE, LOAD, NEWFILE, ADDLAYER, CHANGEVIS, COPY, EXPORT, EXPORTAS,
  GENCHECKERBOARD, OPCOMMAND, REMOVE, SAVE, SAVEAS, SELECT
}
